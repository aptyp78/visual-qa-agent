# 🔍 Visual QA Agent

Автономный агент для визуальной проверки веб-интерфейсов с минимальным участием человека.

## Возможности

- **📱 Мульти-устройства** — проверка на 15+ устройствах (iPhone, Android, iPad, Desktop)
- **🌐 Кросс-браузерность** — Chromium, Firefox, WebKit (Safari)
- **🤖 AI-анализ** — Claude Vision для интеллектуального обнаружения проблем
- **📊 Pixel-perfect сравнение** — обнаружение визуальных регрессий
- **♿ Accessibility** — проверка соответствия WCAG 2.1
- **📈 HTML-отчёты** — интерактивные отчёты с галереей скриншотов

## Установка как MCP-сервер для Claude Code

Это рекомендуемый способ — агент становится доступен как инструмент в любом проекте Claude Code:

```bash
# 1. Клонируем и устанавливаем
cd ~/ai-projects
git clone https://github.com/aptyp78/visual-qa-agent.git
cd visual-qa-agent
npm install
npx playwright install chromium

# 2. Добавляем как MCP-сервер (глобально для всех проектов)
claude mcp add --transport stdio --scope user visual-qa node $(pwd)/src/mcp-server.js
```

После этого в любом Claude Code будут доступны инструменты:

- `mcp__visual-qa__visual_qa_check` — проверка страницы
- `mcp__visual-qa__visual_qa_baseline` — создание baseline
- `mcp__visual-qa__visual_qa_compare` — сравнение скриншотов
- `mcp__visual-qa__visual_qa_analyze` — AI-анализ
- `mcp__visual-qa__visual_qa_devices` — список устройств
- `mcp__visual-qa__visual_qa_audit_clickables` — аудит кликабельных элементов
- `mcp__visual-qa__visual_qa_check_batch` — параллельная проверка нескольких URL
- `mcp__visual-qa__visual_qa_console` — захват логов консоли браузера

## Быстрый старт (CLI)

```bash
# Установка
cd ~/ai-projects/visual-qa-agent
npm install
npx playwright install

# Проверка сайта
npm run check -- https://your-site.com

# Быстрая проверка (3 устройства)
npm run check:quick -- https://your-site.com

# Полная проверка (все устройства и браузеры)
npm run check:full -- https://your-site.com
```

## Команды CLI

```bash
# Проверка страницы
visual-qa check <url> [options]
  -p, --profile <name>  Профиль: quick/standard/comprehensive/mobile_first
  --ai                  Включить AI-анализ (требует ANTHROPIC_API_KEY)
  --compare             Сравнить с baseline
  -o, --output <dir>    Директория для отчёта

# Создание baseline (эталонных скриншотов)
visual-qa baseline <url> [options]
  -p, --profile <name>  Профиль устройств
  -o, --output <dir>    Директория для baseline

# Сравнение двух директорий
visual-qa compare [options]
  -b, --baseline <dir>  Директория baseline
  -c, --current <dir>   Директория текущих скриншотов

# AI-анализ отдельного скриншота
visual-qa analyze <image> [options]
  --accessibility       Проверка accessibility

# Список устройств
visual-qa devices
```

## Профили проверки

| Профиль | Устройств | Браузеров | Время | Когда использовать |
|---------|-----------|-----------|-------|-------------------|
| `quick` | 3 | 1 | ~30 сек | Быстрая проверка во время разработки |
| `standard` | 8 | 2 | ~2 мин | Обычная проверка перед коммитом |
| `comprehensive` | 15+ | 3 | ~5 мин | Полная проверка перед релизом |
| `mobile_first` | 7 | 2 | ~1.5 мин | Приоритет мобильных устройств |

## Стандарты качества

Агент проверяет следующие аспекты:

### Visual Regression
- Pixel-perfect сравнение с baseline
- Порог допустимых различий: 0.1%
- Игнорирование anti-aliasing артефактов

### Layout
- ❌ Вылезание элементов за границы
- ❌ Наложение элементов
- ⚠️ Неправильное выравнивание (допуск 2px)
- ⚠️ Недостаточные отступы (минимум 8px)

### Типографика
- Минимальный размер шрифта: 14px (mobile), 12px (desktop)
- Контраст текста: минимум 4.5:1 (WCAG AA)
- Межстрочный интервал: 1.2 - 2.0

### Responsiveness
- ❌ Горизонтальный скролл
- ⚠️ Touch-targets < 44px
- Проверка на всех breakpoints

### Accessibility (WCAG 2.1 AA)
- Alt-текст для изображений
- Видимый focus indicator
- Информация не только цветом
- Поддержка prefers-reduced-motion

## Консоль браузера (F12 DevTools)

Агент умеет захватывать и анализировать логи консоли браузера:

```bash
# Отдельная проверка консоли
mcp__visual-qa__visual_qa_console url="https://your-site.com"

# Визуальная проверка + консоль
mcp__visual-qa__visual_qa_check url="https://your-site.com" capture_console=true
```

### Что захватывается

| Тип | Описание |
|-----|----------|
| **console.log/warn/error** | Все сообщения консоли |
| **JS Errors** | Необработанные исключения (uncaught exceptions) |
| **Network Errors** | Failed requests, 4xx/5xx responses |
| **Security Warnings** | Mixed content, insecure resources |
| **Performance Metrics** | FCP, Load time, Transfer size |

JS ошибки и серверные ошибки (5xx) автоматически блокируют релиз.

## AI-анализ

Для включения AI-анализа установите API ключ:

```bash
export ANTHROPIC_API_KEY=your-key
```

AI проверяет:
- Общее визуальное качество
- UX-проблемы (непонятная навигация, плохая иерархия)
- Сравнение с baseline (намеренные изменения vs регрессии)
- Accessibility проблемы

## Программное использование

```javascript
import { quickCheck, fullCheck } from 'visual-qa-agent';

// Быстрая проверка
const results = await quickCheck('https://example.com');

// Полная проверка с AI
const results = await fullCheck('https://example.com', {
    profile: 'comprehensive',
    apiKey: process.env.ANTHROPIC_API_KEY
});

console.log(`Passed: ${results.summary.passed}`);
console.log(`Failed: ${results.summary.failed}`);
```

## Структура отчёта

```
reports/
└── latest/
    ├── index.html          # Интерактивный отчёт
    ├── results.json        # JSON для автоматизации
    └── screenshots/        # Все скриншоты
        ├── iphone_14_pro_chromium.png
        ├── laptop_full_hd_firefox.png
        └── ...
```

## Интеграция в CI/CD

```yaml
# GitHub Actions
- name: Visual QA Check
  run: |
    npm run check:full -- ${{ env.STAGING_URL }}
  env:
    ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
```

Exit codes:
- `0` — все проверки пройдены
- `1` — есть ошибки (блокируют релиз)

## Конфигурация

### Устройства: `config/devices.json`
Добавляйте свои устройства или создавайте кастомные профили.

### Стандарты: `config/quality-standards.json`
Настройте пороги, severity levels, игнорируемые области.

## Архитектура

```
src/
├── index.js              # Публичное API (quickCheck, fullCheck)
├── cli.js                # Командная строка
├── mcp-server.js         # MCP сервер для Claude Code
├── core/
│   ├── visual-agent.js   # Ядро: скриншоты, проверки
│   └── issue-detector.js # Детектор DOM-проблем с actionable fixes
├── analyzers/
│   ├── ai-vision-analyzer.js  # Claude Vision анализ
│   └── pixel-comparator.js    # Pixel-perfect сравнение
├── reporters/
│   └── html-reporter.js       # HTML отчёты
└── utils/
    └── helpers.js             # Общие утилиты
```

## MCP Resources

Помимо инструментов, агент предоставляет ресурсы для чтения данных:

| Resource URI | Описание |
|--------------|----------|
| `visual-qa://reports/latest` | JSON-данные последней проверки |
| `visual-qa://config/standards` | Текущие настройки стандартов качества |

Пример использования в Claude Code:
```
Покажи результаты последней проверки из visual-qa://reports/latest
```

## Roadmap

- [ ] Интеграция с Percy/Chromatic для облачного хранения
- [x] Visual diff overlay в отчёте
- [ ] Slack/Teams уведомления
- [x] Параллельный запуск на нескольких URL
- [ ] Запись видео при ошибках
- [x] Dark mode проверки
- [x] Аудит кликабельных элементов
- [x] Rate limiting и timeout для AI API
- [x] Захват консоли браузера (JS ошибки, network failures)

## Лицензия

MIT
